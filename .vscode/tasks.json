{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run",
            "type": "shell",
            "command": "npx web-ext run --devtools --start-url bing.ca"
        },
        {
          "label": "Build popup",
          "type": "shell",
          "command": "cd popup; npm run build"
      }
    ]
}